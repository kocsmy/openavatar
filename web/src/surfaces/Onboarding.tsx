import { EmptyState } from "@/components/live";

/** Placeholder — the Onboarding surface lands with its bridge. */
export default function OnboardingSurface() {
  return <EmptyState title="Onboarding" description="Not built yet." />;
}
