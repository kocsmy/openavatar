import { EmptyState } from "@/components/live";

/** Placeholder — the MenuBar surface lands with its bridge. */
export default function MenuBarSurface() {
  return <EmptyState title="MenuBar" description="Not built yet." />;
}
