import { EmptyState } from "@/components/live";

/** Placeholder — the Call surface lands with its bridge. */
export default function CallSurface() {
  return <EmptyState title="Call" description="Not built yet." />;
}
