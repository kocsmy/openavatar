import { EmptyState } from "@/components/live";

/** Placeholder — the Meetings page lands with its bridge. */
export function MeetingsPage() {
  return <EmptyState title="Meetings" description="Not built yet." />;
}
