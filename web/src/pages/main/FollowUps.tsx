import { EmptyState } from "@/components/live";

/** Placeholder — the FollowUps page lands with its bridge. */
export function FollowUpsPage() {
  return <EmptyState title="FollowUps" description="Not built yet." />;
}
