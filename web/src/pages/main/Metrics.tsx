import { EmptyState } from "@/components/live";

/** Placeholder — the Metrics page lands with its bridge. */
export function MetricsPage() {
  return <EmptyState title="Metrics" description="Not built yet." />;
}
