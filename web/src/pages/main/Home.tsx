import { EmptyState } from "@/components/live";

/** Placeholder — the Home page lands with its bridge. */
export function HomePage() {
  return <EmptyState title="Home" description="Not built yet." />;
}
